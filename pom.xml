<dependencies>
    <!-- Spring Context for DI (Dependency Injection) -->
    <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-context</artifactId>
        <version>5.3.25</version> <!-- Ensure this is the latest stable version -->
    </dependency>

    <!-- Spring Beans for Bean creation -->
    <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-beans</artifactId>
        <version>5.3.25</version> <!-- Ensure this is the latest stable version -->
    </dependency>

    <!-- Log4j for logging (optional but recommended) -->
    <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-logging</artifactId>
        <version>5.3.25</version>
    </dependency>

    <!-- JUnit for testing -->
    <dependency>
        <groupId>junit</groupId>
        <artifactId>junit</artifactId>
        <version>4.13.2</version>
        <scope>test</scope>
    </dependency>
</dependencies>
